# IMPORT-05/07 bounded confirmation validation

September 12, 2026, isolated delivery worktree from e0815c3. Local implementation; release verification pending.

The receipt callback no longer starts or awaits full collection/tag reads. Loader invalidation removes the account cache and labels ownership stale. Consumers coalesce active reads, reject older generations and can retry failures. Home, Collection, catalog and restored views reconcile lazily. No backend preparation/read-scope optimization is claimed for IMPORT-06/08–10.

143 unit tests and the app build passed. The focused Chromium set passed 30 of 31 initially; its sole failure expected the old extra collection read. That assertion was updated to the required zero and all seven phone-review cases passed on rerun. Ten targeted WebKit cases passed. Tests include fifty-row lost-response retry, unrelated pending drafts, empty/failure/cache account isolation, chronological scanner batches and Recent exact-printing receipts. Three new consumer tests deliberately stall collection I/O and prove confirmation still completes with zero reads, then navigation triggers one read. Required broad CI and deployed LIVE-02 assertions remain pending.

Controlled timing: one Chromium observation per variant, same isolated SQLite fixture and an injected 500 ms collection-response delay. Baseline app/loader source is read directly from e0815c3. Backend-plus-transport interval (including browser click dispatch) was 142.27 ms before and 127.62 ms after. Post-response UI completion was 819.73 ms before and 24.03 ms after; total confirmation 962.00 ms versus 151.65 ms. Collection requests after receipt: one before, zero after. This demonstrates the removed dependency; it is neither a backend speedup nor a production latency estimate. No statistical confidence or physical-device result is claimed.

Repeat explicitly with `npx playwright test --config tests/performance/import-timing.config.mjs` from a checkout containing baseline commit e0815c3. It is excluded from ordinary app E2E runs; result JSON is attached to each test's output directory. Synthetic owner `test`, in-memory SQLite, no owner credentials or collection data.

Independent reviewer cycle dbc379cc-5c52-417c-b663-1916cd367b4b found the missing scroll/order acceptance and requested changes. The follow-up captures the user's current offsets at redraw, restores them only on the same active Import generation, and preserves natural browser clamping when the page shrinks. Five focused Chromium cases pass, including twenty unrelated drafts and navigation away during a held consumer read. The review also resolved all three earlier CI source/publication findings using supplied implementation evidence; no CI optimization is adopted. New commit requires independent re-review before release.
