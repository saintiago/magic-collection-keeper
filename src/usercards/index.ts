/**
 * UserCards public entry point.
 *
 * Other components import UserCards through this module only; its internal modules stay private to
 * the component (docs/architecture.md, .dependency-cruiser.mjs).
 */
export {};
