/**
 * Application public entry point.
 *
 * Other components import Application through this module only; its internal modules stay private
 * to the component (docs/architecture.md, .dependency-cruiser.mjs).
 */
export {};
