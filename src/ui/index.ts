/**
 * UserInterface public entry point.
 *
 * Other components import UserInterface through this module only; its internal modules stay private
 * to the component (docs/architecture.md, .dependency-cruiser.mjs).
 */
export {};
