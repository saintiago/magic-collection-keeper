{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Description": "Keeper SageMaker comparison only. No provisioned concurrency, inventory access, S3 model bucket, CI role edits or pantry resources.",
  "Parameters": {
    "ImageUri": {
      "Type": "String",
      "AllowedPattern": "^698643713254\\.dkr\\.ecr\\.us-east-1\\.amazonaws\\.com/magic-keeper-recognition@sha256:[0-9a-f]{64}$"
    }
  },
  "Resources": {
    "ModelLogs": {
      "Type": "AWS::Logs::LogGroup",
      "Properties": {
        "LogGroupName": "/aws/sagemaker/Endpoints/magic-keeper-sagemaker",
        "RetentionInDays": 7
      }
    },
    "ProxyLogs": {
      "Type": "AWS::Logs::LogGroup",
      "Properties": {
        "LogGroupName": "/aws/lambda/magic-keeper-sagemaker-proxy",
        "RetentionInDays": 7
      }
    },
    "ModelRole": {
      "Type": "AWS::IAM::Role",
      "Properties": {
        "AssumeRolePolicyDocument": {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Principal": {
                "Service": "sagemaker.amazonaws.com"
              },
              "Action": "sts:AssumeRole",
              "Condition": {
                "StringEquals": {
                  "aws:SourceAccount": {
                    "Ref": "AWS::AccountId"
                  }
                },
                "ArnLike": {
                  "aws:SourceArn": {
                    "Fn::Sub": "arn:${AWS::Partition}:sagemaker:${AWS::Region}:${AWS::AccountId}:*"
                  }
                }
              }
            }
          ]
        },
        "Policies": [
          {
            "PolicyName": "IsolatedRecognition",
            "PolicyDocument": {
              "Version": "2012-10-17",
              "Statement": [
                {
                  "Effect": "Allow",
                  "Action": ["ecr:GetAuthorizationToken"],
                  "Resource": "*"
                },
                {
                  "Effect": "Allow",
                  "Action": [
                    "ecr:BatchGetImage",
                    "ecr:GetDownloadUrlForLayer",
                    "ecr:BatchCheckLayerAvailability"
                  ],
                  "Resource": {
                    "Fn::Sub": "arn:${AWS::Partition}:ecr:${AWS::Region}:${AWS::AccountId}:repository/magic-keeper-recognition"
                  }
                },
                {
                  "Effect": "Allow",
                  "Action": ["logs:CreateLogStream", "logs:PutLogEvents"],
                  "Resource": {
                    "Fn::Sub": "arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/sagemaker/Endpoints/magic-keeper-sagemaker:*"
                  }
                }
              ]
            }
          }
        ]
      }
    },
    "ProxyRole": {
      "Type": "AWS::IAM::Role",
      "Properties": {
        "AssumeRolePolicyDocument": {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Principal": {
                "Service": "lambda.amazonaws.com"
              },
              "Action": "sts:AssumeRole"
            }
          ]
        },
        "Policies": [
          {
            "PolicyName": "IsolatedRecognition",
            "PolicyDocument": {
              "Version": "2012-10-17",
              "Statement": [
                {
                  "Effect": "Allow",
                  "Action": ["sagemaker:InvokeEndpoint"],
                  "Resource": {
                    "Fn::Sub": "arn:${AWS::Partition}:sagemaker:${AWS::Region}:${AWS::AccountId}:endpoint/magic-keeper-sagemaker"
                  }
                },
                {
                  "Effect": "Allow",
                  "Action": ["logs:CreateLogStream", "logs:PutLogEvents"],
                  "Resource": {
                    "Fn::Sub": "arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/lambda/magic-keeper-sagemaker-proxy:*"
                  }
                }
              ]
            }
          }
        ]
      }
    },
    "Model": {
      "Type": "AWS::SageMaker::Model",
      "Properties": {
        "ExecutionRoleArn": {
          "Fn::GetAtt": ["ModelRole", "Arn"]
        },
        "PrimaryContainer": {
          "Image": {
            "Ref": "ImageUri"
          },
          "Environment": {
            "ORT_DISABLE_TELEMETRY": "1"
          }
        }
      }
    },
    "EndpointConfig": {
      "Type": "AWS::SageMaker::EndpointConfig",
      "Properties": {
        "ProductionVariants": [
          {
            "VariantName": "AllTraffic",
            "ModelName": {
              "Fn::GetAtt": ["Model", "ModelName"]
            },
            "ServerlessConfig": {
              "MemorySizeInMB": 3072,
              "MaxConcurrency": 2
            }
          }
        ]
      }
    },
    "Endpoint": {
      "Type": "AWS::SageMaker::Endpoint",
      "DependsOn": "ModelLogs",
      "Properties": {
        "EndpointName": "magic-keeper-sagemaker",
        "EndpointConfigName": {
          "Fn::GetAtt": ["EndpointConfig", "EndpointConfigName"]
        }
      }
    },
    "Proxy": {
      "Type": "AWS::Lambda::Function",
      "DependsOn": "ProxyLogs",
      "Properties": {
        "FunctionName": "magic-keeper-sagemaker-proxy",
        "Runtime": "python3.12",
        "Handler": "index.handler",
        "MemorySize": 256,
        "Timeout": 28,
        "Role": {
          "Fn::GetAtt": ["ProxyRole", "Arn"]
        },
        "Environment": {
          "Variables": {
            "ENDPOINT_NAME": {
              "Fn::GetAtt": ["Endpoint", "EndpointName"]
            }
          }
        },
        "Code": {
          "ZipFile": "\"\"\"JWT-to-SigV4 transport; no ML imports or inventory permissions.\"\"\"\n\nimport base64\nimport json\nimport os\n\nimport boto3\nfrom botocore.config import Config\nfrom botocore.exceptions import BotoCoreError, ClientError\n\nruntime = boto3.client(\n    \"sagemaker-runtime\",\n    config=Config(\n        connect_timeout=2, read_timeout=25, retries={\"total_max_attempts\": 1}\n    ),\n)\n\n\ndef response(code, value):\n    return {\n        \"statusCode\": code,\n        \"headers\": {\"content-type\": \"application/json\", \"cache-control\": \"no-store\"},\n        \"body\": json.dumps(value),\n    }\n\n\ndef handler(event, context):\n    owner = (\n        event.get(\"requestContext\", {})\n        .get(\"authorizer\", {})\n        .get(\"jwt\", {})\n        .get(\"claims\", {})\n        .get(\"sub\")\n    )\n    if not isinstance(owner, str) or not owner:\n        return response(401, {\"error\": \"Sign in to scan\"})\n    body = event.get(\"body\")\n    if event.get(\"isBase64Encoded\") or not isinstance(body, str) or len(body) > 710000:\n        return response(413, {\"error\": \"Image request too large\"})\n    try:\n        payload = json.loads(body)\n        if not isinstance(payload, dict) or set(payload) != {\"image\", \"attempt\"}:\n            raise ValueError()\n        if type(payload[\"attempt\"]) is not int or not 1 <= payload[\"attempt\"] <= 100000:\n            raise ValueError()\n        if not isinstance(payload[\"image\"], str):\n            raise ValueError()\n        if not 0 < len(base64.b64decode(payload[\"image\"], validate=True)) <= 524288:\n            return response(413, {\"error\": \"Image request too large\"})\n    except (ValueError, TypeError):\n        return response(400, {\"error\": \"Invalid image request\"})\n    try:\n        result = runtime.invoke_endpoint(\n            EndpointName=os.environ[\"ENDPOINT_NAME\"],\n            ContentType=\"application/json\",\n            Body=body.encode(),\n            CustomAttributes=context.aws_request_id,\n        )\n        with result[\"Body\"] as stream:\n            value = json.loads(stream.read(65537))\n        if (\n            value.get(\"contractVersion\") != 1\n            or value.get(\"attempt\") != payload[\"attempt\"]\n            or value.get(\"status\") not in (\"unknown\", \"possible\")\n        ):\n            raise ValueError()\n        return response(200, value)\n    except ClientError as error:\n        code = error.response.get(\"Error\", {}).get(\"Code\")\n        if code in (\"ModelNotReadyException\", \"ServiceUnavailable\"):\n            return response(\n                503,\n                {\"error\": \"Recognition is preparing. Retry shortly\", \"preparing\": True},\n            )\n        if code in (\"ThrottlingException\", \"TooManyRequestsException\"):\n            return response(429, {\"error\": \"Scanner busy. Retry this card\"})\n        return response(503, {\"error\": \"Recognition unavailable. Retry this card\"})\n    except (BotoCoreError, ValueError, KeyError):\n        return response(503, {\"error\": \"Recognition unavailable. Retry this card\"})\n"
        }
      }
    },
    "Integration": {
      "Type": "AWS::ApiGatewayV2::Integration",
      "Properties": {
        "ApiId": "exex6mzt02",
        "IntegrationType": "AWS_PROXY",
        "IntegrationUri": {
          "Fn::GetAtt": ["Proxy", "Arn"]
        },
        "PayloadFormatVersion": "2.0",
        "TimeoutInMillis": 29000
      }
    },
    "Route": {
      "Type": "AWS::ApiGatewayV2::Route",
      "Properties": {
        "ApiId": "exex6mzt02",
        "RouteKey": "POST /api/recognize/sagemaker",
        "AuthorizationType": "JWT",
        "AuthorizerId": "giiovd",
        "Target": {
          "Fn::Join": [
            "/",
            [
              "integrations",
              {
                "Ref": "Integration"
              }
            ]
          ]
        }
      }
    },
    "Permission": {
      "Type": "AWS::Lambda::Permission",
      "Properties": {
        "Action": "lambda:InvokeFunction",
        "FunctionName": {
          "Ref": "Proxy"
        },
        "Principal": "apigateway.amazonaws.com",
        "SourceArn": {
          "Fn::Sub": "arn:${AWS::Partition}:execute-api:${AWS::Region}:${AWS::AccountId}:exex6mzt02/*/POST/api/recognize/sagemaker"
        }
      }
    }
  }
}
