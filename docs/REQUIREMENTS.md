# Product requirements

This document records owner intent, including queued work. It is not proof of production behavior. Latest owner corrections override earlier decisions. Update it for every feature request or correction; connect implemented requirements to use cases, tests and release evidence.

## Status and traceability

QUALITY-01 verification correction: workflow 34427837906 again passed all predeployment checks and 10/11 desktop live cases, but LIVE-17's cleanup attempted Sign out through an open artwork dialog and masked the original scenario failure. Mobile did not run. Test cleanup must close the test page safely and preserve the scenario error; phase diagnostics must distinguish isolated fixtures from real cloud runs. The complete mouse/touch scenario is now rehearsed through the same assertions against isolated application ports. A main-only verification workflow targets an explicit already-published commit/release, uses the existing reserved profiles under the publication lock, and repeats both live engines without changing the application. This operational correction does not establish production success; the cloud failure still needs diagnosis.

Use **queued**, **in progress**, **locally verified**, or **production verified**. Production verification requires an identified release and relevant live evidence. The entries below capture the active interaction revision and known backlog on September 9, 2026. The card opening/hover/wheel revision is production verified in release r88-a1. The r114 checkpoint below identifies the later delivered work and supersedes its earlier local-progress notes; other queued requests remain unchanged.

Current release evidence: **r114-a1**, main `5ba64746bbf858cf8072f90a14b3e0869d456624`, passed [normal main workflow 34432477877](https://github.com/saintiago/magic-collection-keeper/actions/runs/34432477877): 131 unit, 139 Chromium, 103 WebKit (one existing unsupported-stream skip), 56 visual, 12 actual desktop and eight actual mobile live cases. Independent identity/source/cleanup checks and six served-layout cases passed; twelve screenshots were inspected. RECENT-01–04, VIEW-04, CARD-10–17 and DRAG-04/05 are production verified within the recorded browser/cloud coverage. SCAN-03/04 implementation is deployed and passes automated/synthetic checks; physical reflective-card, camera, audio and identical-copy acceptance remains unverified. [Detailed evidence and limits](../tests/performance/R114-RELEASE.md). Earlier r103/r107 failures and local-only statuses below are historical and superseded by this checkpoint for these requirements only.

## Card interaction revision — production verified r88-a1

| ID         | Requirement and acceptance criteria                                                                                                                                                                                                                                                                                                    |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CARD-01    | All card grids show card images without metadata underneath; use narrower gaps. Preserve accessible names.                                                                                                                                                                                                                             |
| CARD-02    | Pointer movement continuously tilts the card toward the pointer. Hover immediately reveals collection-specific information; after **300 ms**, lift to 200% of the rendered tile size. Leaving before the delay cancels the lift. Tilt remains active on the enlarged card.                                                             |
| CARD-03    | Click/tap enlarges the card **in place over the visible page**, preserving its source slot, filters and scroll. Target 300% of the rendered tile size, capped initially to show the complete card with usable outside-dismissal margins. Shift minimally for viewport edges; never unconditionally center on the screen.               |
| CARD-04    | Information stays near the card's left edge and actions near its right edge. Smaller screens allow progressively more overlap while keeping the card's center and dismissal margins usable. No detached screen-edge controls or encompassing control panel.                                                                            |
| CARD-05    | Do not repeat printed card name, rules, type, set or collector number in hover/zoom overlays. Show collection-specific information and actions. Tags display plain names without decorative squares or quantity suffixes. Preserve underlying ownership and location quantity semantics.                                               |
| CARD-06    | Controls use restrained translucent glass surfaces, illuminated edges, subtle action-specific tints and interaction-responsive light/depth. Dark shadow blobs, opaque rings and generic large button panels do not satisfy the visual requirement. Load licensed fonts deliberately; verify actual font loading and readable fallback. |
| CARD-07    | Zoom uses smooth overshoot-and-settle spring motion. Keep a single solid lifted card rather than overlapping translucent copies. Avoid layout jumps, pointer stutter, text selection and native image dragging during active drag. Restore normal selection afterward.                                                                 |
| CARD-08    | Wheel/pinch adjusts zoom; deliberate enlargement permits bounded panning. Outside click/tap closes and consumes the gesture without activating underlying content. Reset restores the comfortable initial view; preserve keyboard dismissal and reduced-motion behavior.                                                               |
| DRAG-01    | Dragging a card directly reveals a radial action wheel around the selected card. No three-dot/dropdown trigger. Use true sectors, an open center and readable unclipped targets.                                                                                                                                                       |
| DRAG-02    | Recent/relevant tags are easiest to reach; less relevant actions remain available through outer choices or More. Freeze target geometry during a gesture. Maintain continuous outward movement to select an action.                                                                                                                    |
| DRAG-03    | Keep the selected card unmistakable in the wheel center; adapt once for viewport edges. The moving card becomes translucent over targets. Do not obscure labels or substitute a tiny moving thumbnail. Preserve quantity, ownership and tag-ID rules.                                                                                  |
| PERF-01    | Compare actual visual implementations, including the WebGL experiment, using equivalent interactions. Choose by visual result and measured responsiveness, not renderer name. Test dense grids, narrow phones, landscape, wide screens, interruption and failure paths. Do not describe emulation as physical-device verification.     |
| QUALITY-01 | Iterate locally and inspect real screenshots and motion before release. Passing tests alone is insufficient visual acceptance. Integrate and deploy through the normal workflow when the requirements are met; record remaining limitations honestly.                                                                                  |

Superseded: 200 ms hover delay; unconditional 300% opening on phones; viewport-centered click inspector; repeated printed metadata; small moving drag thumbnail; wheel opened through an actions menu.

### Current evidence

CARD-01–08 and DRAG-01–03 shipped in merged PR #13, main 6c94f2505b467ceb26c905f35bd42b79559baa00, release 0.1.0+deploy.88.1. All 18 deployed tests passed; twelve actual served-asset cases across Chromium/WebKit at six viewport sizes verified anchoring, full fit, fonts, dismissal and restoration using read-only synthetic rows. CARD-02 targets 200% subject to a 16px viewport edge fit; CARD-03 targets 300% subject to 24px horizontal/32px vertical safe-area dismissal gutters. CARD-07 covers the zoom spring; flip remains queued as CARD-09.

Traceability: [card use cases](USE-CASES.md), [layout/gesture architecture](ARCHITECTURE.md), [application interaction tests](../tests/ui/card-actions.spec.js), [prototype geometry and timing tests](../tests/prototype/geometry.spec.js), [font/glass/failure checks](../tests/prototype/fit-fonts.spec.js), and [measured dense-grid limits and historical comparisons](../tests/performance/CARD-ACTIONS.md). Actual public-image v18 wide/phone captures were independently reviewed before release. PERF-01 records Chromium 16.7–16.8ms frame p95 and WebKit drag 30ms p95 on this host; physical hardware is unverified. QUALITY-01 is production verified for that revision: exact corresponding runtime source was published and checked, and workflow 34401002081 succeeded. These browser checks do not establish physical-device behavior.

## Queued views and features

| ID      | Requirement and acceptance criteria                                                                                                                                                                                                                                                                                                                                                                                                     | Status      |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| VIEW-01 | Add sorting by card type. Selecting a deck tag enables the deck-oriented view with card-type sorting.                                                                                                                                                                                                                                                                                                                                   | queued      |
| VIEW-02 | Add a format tag family, including Commander and Modern, and a commander role tag. Labels remain editable metadata backed by stable IDs.                                                                                                                                                                                                                                                                                                | queued      |
| VIEW-03 | When the nonempty full filtered result is explicitly tagged Commander format, show commander-role cards first with a prominent golden frame/wrapping. It must be clearly distinguishable at normal grid size on desktop and phone, without hovering or obscuring card content; a faint border alone is insufficient. This supersedes the earlier subtle-border treatment. Do not infer this view from legality or only the loaded page. | queued      |
| VIEW-04 | Display whole-collection total statistics only on Home, after the user's content; omit that block from catalogue, collection and deck views.                                                                                                                                                                                                                                                                                            | production verified |
| CARD-09 | True double-sided cards expose a flip action with overshoot-and-settle animation. Handle rapid input, image loading/failure and composition with zoom/tilt without changing ownership.                                                                                                                                                                                                                                                  | queued      |

VIEW-04 acceptance: the total-copies, unique-printings, sets and foil/etched block appears below Home's recent cards and deck/tag sections. It is absent from collection, catalogue, tag/deck, Import and dedicated card views, including direct links, reload and Back. Its values continue to use the complete account collection; filters and pending imports cannot change those totals. Unknown loading uses placeholders, failed refresh retains explicitly identified saved state, and account changes cannot expose the previous account's values. Locally verified: ten Home/loading cases pass in Chromium and WebKit, including account isolation and unavailable/saved data. Final mobile/desktop layout captures and route checks pass in both engines after adding spacing below the Home content; the screenshots were inspected. Home unit tests and build pass. LIVE-10 passed against r114-a1 for actual Home totals and placement; independent served-layout checks also passed at 1280, 390 and 320 px in Chromium and WebKit.

## Scanner corrections — in progress

Duplicate capture (SCAN-03/04) has priority over split-card handling and the deployment-speed follow-up. No existing draft or owner data should be changed automatically.

- **SCAN-01:** Treat a single physical split card (for example Order // Chaos) as one card even when its printed panels resemble separate cards. Count physical card boundaries rather than printed panels. Accept supported rotations and resolve the combined printing identity as one capture/copy.
- **SCAN-02:** Preserve rejection of two distinct or overlapping physical cards. Do not fix split-card rejection by globally disabling or weakening the one-card gate. Verify split cards, ordinary cards, two separate cards and overlaps with regression fixtures. Distinguish screenshot replay from physical-camera evidence.
- **SCAN-03:** A physical card remaining in the capture area must produce at most one queued copy and one success cue. Glare, foil reflections, autofocus/exposure changes, small movement, transient detection loss and late parallel recognizer responses must not rearm capture. Investigate the reported repeated Elsha, Threefold Master entries; the screenshot alone does not establish the cause.
- **SCAN-04:** Rearm only on validated physical departure/replacement, independently of recognized name. A subsequent physical copy of the same identity must remain scannable. Test stationary reflective cards, transient occlusion/detection loss, actual removal/re-entry, identical consecutive copies and out-of-order recognizer results. Do not silently delete existing draft rows or deduplicate by card name; preserve user review and ownership confirmation.
- **SCAN-05:** Remove the user-facing 50-card session stop. Separate continuous scanning from bounded storage/commit chunks, retaining explicit ownership review, resumable drafts, bounded in-memory work, retry idempotency and visible persistence errors. Verify scanning beyond 50 cards without loss, duplicate commits or forced Review. This is a requested workflow improvement, not an assertion that backend limits can simply be removed.

- **SCAN-06:** Replace routine textual recognition-stage/status overlays with spatial visual feedback: colored translucent masks, outlines and transitions highlighting the relevant card regions. Distinguish framing, processing and result states without obscuring the artwork. Do not invent region localization or processing stages; use actual available geometry and events. Retain accessible status equivalents and concise actionable permission/network/error messages where visual effects alone cannot explain recovery. Respect reduced motion and do not rely on color alone.
- **SCAN-07:** Beside each queued card name, show compact icons for the actual recognition sources that have returned evidence for that capture. Map icons to real configured providers rather than guessed architecture names; provide accessible labels/tooltips. Results arriving after the camera has moved to another card update their original row by capture ID, never the current card. No extra copy or success cue from a late source result; never overwrite user-reviewed choices. Distinguish returned alternatives/failure from corroboration rather than implying every response agrees. Test out-of-order replies, navigation/reload as applicable, and scanning the next card while earlier evidence arrives.

## Shared Import card grid — queued after scanner corrections

- **IMPORT-01:** Present pending imports through the same image-only card grid and interaction components as the catalogue, selected by pending-import system state (`system:import-pending`). Reuse immediate collection/review information on hover, 300 ms dwell to 200% zoom with tilt, in-page click/tap enlargement, responsive glass controls and consumed outside dismissal. Replace the always-expanded form rows shown in the current Import view.
- **IMPORT-02:** Supply import-specific actions to the shared interaction components: review/change suggested printing or recognition alternative, edit draft quantity/finish/condition/tags, remove a draft item, and explicitly confirm ownership. Do not apply owned-card mutations to pending items. Keep errors and unresolved readings actionable without cluttering every tile with forms.
- **IMPORT-03:** Order pending cards by immutable creation time, oldest first (scan/import sequence), with stable tie-breaking. Editing or recognition refinements must not reorder them. Retain date grouping where useful without reversing the within-group sequence; do not fabricate timestamps for legacy entries.
- **IMPORT-04:** Preserve persisted drafts, source provenance, retry idempotency and explicit Add semantics. Pending items remain excluded from owned totals until confirmation succeeds. Treat the tag as system lifecycle state, not an ordinary removable user label that bypasses confirmation. Reuse navigation/filter/scroll behavior; test reload, partial failures and long sessions.

## Frontend deployment workflow — delivered and verified

- **DEPLOY-01:** Provide a frontend-only path through the existing main deployment workflow for eligible UI changes. Publish versioned frontend assets without rebuilding or updating unchanged application/recognition backends or model assets.
- **DEPLOY-02:** Determine eligibility from the change set and explicit compatibility rules; backend, API-contract, recognition or infrastructure changes require the full path. Unknown changes must not silently take the fast path.
- **DEPLOY-03:** Keep frontend and backend release identities independently accurate, retain atomic HTML publication and rollback/version ordering, and preserve the complete corresponding source offer for the deployed combination.
- **DEPLOY-04:** Run meaningful frontend/browser/build checks and live compatibility smoke checks using separate test profiles. Measure total deployment time against the full workflow. Reuse verified immutable artifacts; do not weaken integrity checks, permissions or owner-data protection to gain speed.

DEPLOY-01–04 status: production delivery verified in `r91-a1` following the full `r90-a1` bootstrap. The separate frontend run passed all selected browser/live checks, reused the exact API code and last-modified timestamp, recognition version and vendor manifest, and preserved the matching combined source download. Observed elapsed time: 13m 00s versus 18m 18s, saving 5m 18s (29.0%). [Detailed evidence, test coverage and measurement limits](../tests/performance/FRONTEND-DEPLOYMENT.md). Rollback guards have local regression coverage; production rollback was not intentionally triggered.

## Zoom return — in progress

- **CARD-10:** Outside-click/tap dismissal of the enlarged card must visibly shrink and translate the same card back into its original grid frame with smooth overshoot-and-settle motion, rather than abruptly disappearing. Keep the source tile hidden until the returning card reaches it, then restore it without a duplicate image or flash. Preserve the now-approved opening behavior. Consume the dismissing input so it cannot activate another card/control. Resolve the destination from the current source frame; handle scrolling, interrupted/rapid input, missing source tiles and reduced-motion preference safely. Verify intermediate closing frames and final alignment on desktop and phone, including edge cards; do not claim physical-phone verification from emulation.

CARD-10 verification remains in progress: PR run 34420482350 passed the application Chromium and required WebKit checks twice, but each attempt missed intermediate shrinking frames in one desktop WebKit prototype case. Ten unchanged local repeats of the first failing edge case passed. The return test now records frame times, layer transforms and bounds on assertion failure; the visible-shrink and exact-landing criteria are unchanged. The application revision is not production delivered.

Hosted frame diagnostics reproduced early 96–243 ms paint stalls; the repeated Linux baseline failed 14 of 40 return cases, and preparing the moving layers alone still failed five. The correction keeps those two transient layers prepared and limits return progress to 32 ms per rendered step, retaining the normal 380 ms spring with a 1200 ms wall-time bound before exact handoff. The opening transforms are unchanged. A controlled 180 ms main-thread stall now checks visible shrink, same-image/source hiding, bounded completion, final alignment and focus. Eighteen local cross-engine return cases pass; hosted repeated and full-release verification remain pending. These are browser fixtures, not physical-device evidence.

The corrected repeated Linux WebKit run [34424372478](https://github.com/saintiago/magic-collection-keeper/actions/runs/34424372478) passed all 45 return cases, including five stalled-frame repetitions. The 128 unit tests and build pass. Full application release and physical-device verification remain pending.

## Recent cards — production verified r114-a1

- **RECENT-01:** Populate Recent cards from both cards clicked/tapped to open in any card list and cards successfully added to the collection. Opening the in-page zoom counts; hover, background recognition and merely loading a list do not. Include catalogue, collection, tag/deck-filtered and pending-import lists wherever cards can be opened.
- **RECENT-02:** Order by the most recent qualifying open or successful addition, newest first. Opening or adding an already-listed printing moves its existing entry to the front rather than duplicating it. Retain exact-printing identity; this history does not change owned quantities, source provenance or pending-import status. Failed/cancelled additions do not count as additions.
- **RECENT-03:** Keep history bounded, persistent across reload and isolated by account. Verify entry from every shared list, repeated opens, successful/failed additions, ordering and account switching. Pending cards opened from Import may appear as recently viewed but remain pending and excluded from owned totals.

Exact-printing matching governs Home artwork and card-action targets as well as deduplication: owning another printing must never redirect a Recent gesture to that inventory row. Existing identity-level search badges remain a separate search contract. This supersedes the earlier Home Oracle-identity deduplication and owned-artwork fallback.

Local evidence: [Home unit tests](../tests/home.test.js) cover exact-printing order, bounded persistence and account isolation. [Recent browser cases](../tests/ui/recent-printing.spec.js) verify exact unowned artwork, an unselected tag despite another owned printing's assignment, and a tag action staging the selected printing rather than mutating the other inventory row. Its deliberately interrupted save remains visible and returns focus without changing ownership. These pass in Chromium and WebKit with isolated fixtures; deployed verification remains pending.

- **RECENT-04:** Selecting a card from search autocomplete, by click/tap or keyboard confirmation, is a qualifying Recent event even when that selection follows a different path from opening a list tile. Move the selected card to the front using the same identity/deduplication rules. Merely typing a query, receiving suggestions or highlighting a suggestion does not add every suggested card. Verify mouse, touch and keyboard selection and avoid recording the same selection twice through overlapping handlers.

Confirmed mouse/touch/keyboard suggestions record immediately; later detail loading enriches an existing entry without moving it or inserting an unchosen card. Reload/deep-link restoration does not create activity. A successful explicit Add on the dedicated card page records the exact printing; a failed Add does not. These event cases pass in both browser engines. These follow-up behaviors are production verified in r114-a1; see the current release evidence.

Import-batch addition acceptance: the atomic import result/receipt must identify only printings whose owned quantity actually increased. Home must record those exact reviewed cards after confirmed success, preserving stable batch order and deduplication. An unchanged reimport or an allocation-only change creates no addition activity; failed/stale/account-changed responses create none. A replay returns the original confirmed printing IDs without adding copies again. Older receipts without this metadata must not cause the browser to guess from aggregate counts.

The follow-up now returns `added_printings` from actual source-lot quantity increases and stores that list atomically in the import receipt. Confirmed Import success records only matching reviewed cards before the independent collection refresh. All 131 unit cases pass, including exact additions, retained other printings, unchanged reimport, replay and owner isolation. Two new Chromium and WebKit cases pass for a lost response/retry followed by unchanged reimport, and for an older receipt with missing metadata. Broad application checks, source publication and deployed verification remain pending; these changes require a matching new backend source release.

The shared-origin regression also passes in Chromium and WebKit: catalogue, collection, tag-filtered, pending-import and Home artwork opens update exact-printing order, while a visible hover does not. Pending/Home opens retain the pending printing's thumbnail and leave its draft quantity, owned totals and import-write count unchanged. Home accepts the existing pending-summary image field for addition metadata. The complete Chromium run passed 131 cases; two missing generated-public-fixture failures were rerun successfully with the prepared fixtures (three affected model/geometry cases passed). The required WebKit selection plus eight new Recent cases passed 97 tests with the existing unsupported camera-stream case skipped. The additional touch rehearsal of LIVE-19/20 passed in Chromium and WebKit against an isolated application/database fixture. [LIVE-19](../tests/live/recent.spec.js) and [LIVE-20](../tests/mobile-live/recent.spec.js) are prepared to verify real import receipt replay, exact Home artwork, pending ownership and reload order using only the reserved test profile; both passed against production in r114-a1.

## Tags-only inspector — in progress, supersedes CARD-04/05 overlay content

- **CARD-11:** Supersede the earlier enlarged-card information/action layout with relevant tags only. Remove displayed quantity/finish/condition summaries and Qty/Save, Edit tags, Details, zoom percentage/plus/minus and close-button controls from this overlay. Preserve in-page enlargement, wheel/pinch zoom and consumed outside dismissal with CARD-10 return animation. Other dedicated workflows remain available; this does not remove their data or capabilities.
- **CARD-12:** Show all available relevant tags, both assigned and unassigned, with a clear selected/unselected distinction that is not color alone. Always include currently assigned user tags. Click/tap toggles the selected tag directly; support keyboard activation and accessible pressed state. Keep labels plain, with no quantity suffix or decorative square. Keep the card legible and tags reachable on small screens without clipping; overflow must not silently hide available tags.
- **CARD-13:** Persist tag toggles through existing stable tag IDs and assignment semantics. Preserve location quantities, owned totals and provenance; do not treat system lifecycle tags as user-toggleable tags or bypass pending-import confirmation. Show pending/error state on the affected tag, reconcile rapid toggles safely, and restore the prior state on failure. Verify assigned/unassigned rendering, add/remove/reload, failure and rapid input, account isolation, and location/system-tag constraints. Status: production verified in r114-a1 within automated browser/cloud coverage; physical-device appearance and performance remain unverified.

## Shared zoom/wheel tag controls — in progress

- **CARD-14:** The 200% hover enlargement and click/tap enlargement use the same relevant tags, assigned states and direct add/remove behavior from CARD-11–13. Only their arrangement differs to suit the available space and interaction mode; neither zoom level restores the superseded information/control blocks. Keep assigned state synchronized across both views after changes.

  Local implementation evidence for CARD-11–14 (not production delivery): [tag state regressions](../tests/card-tag-state.test.js) cover renamed labels, authoritative replay results, serialized rapid intent, independent failed choices and stale loads. [Browser tag controls](../tests/ui/card-tags.spec.js) cover plain selected states, hover-to-click continuity, persisted assignments, preserved location quantities/owned totals and hidden replacement tiles during return. [Browser retry boundaries](../tests/ui/card-tag-retry.spec.js) cover a lost catalogue stage response followed by reload into pending, bounded/corrupt reference storage and honest post-commit storage failure. Remaining wheel/input-mode integration and full browser/cloud verification are outstanding.

- **DRAG-04:** The radial wheel uses the same relevant tag set and assignment semantics as the zoom overlays. An assigned tag target explicitly reads `Remove [tag name]`; an unassigned target reads `Add [tag name]`. Dropping performs the indicated operation once and updates all presentations. Freeze target layout and the indicated operation during an active gesture; reconcile stale state safely without inverting the user's intended action. Preserve location quantities, system-tag restrictions, failure recovery and accessible alternatives.
- **DRAG-05:** Dragging must start directly from the already enlarged 200% hover card, including while its enlargement is animating. The user must not need to leave hover or wait for the card to shrink. Preserve pointer capture, physical pointer-to-card continuity and the existing wheel-around-card gesture, suppress native image/text dragging, and prevent the drag/release from opening click zoom or toggling an unintended tag. Test pickup before, during and after the 300 ms dwell transition, pointer movement across overlay targets, and cancellation. Status: production verified in r114-a1 within automated browser/cloud coverage; physical-device appearance and performance remain unverified.

## Touch and desktop presentation — latest interaction contract

- **CARD-15:** Mobile/touch tap opens an in-page 200% target enlargement and displays the radial tag wheel. Wheel targets support tap to perform their indicated Add/Remove operation as well as the existing drag interaction. Preserve full-card fit, usable outside-dismiss area and return animation. This supersedes touch tap opening the desktop 300% presentation. Avoid double activation from compatibility mouse events; normal touch scrolling must not open the card.
- **CARD-16:** Desktop mouse behavior remains 300 ms hover to 200% with tilt and click to 300% target enlargement with glass tag controls. Preserve drag pickup from hover enlargement. Choose behavior by actual input capability/event rather than viewport width alone so touch laptops and narrow desktop windows remain usable. Both input modes share relevant tags, assigned state, persistence and error semantics, but use the requested different presentation.
- **CARD-17:** Refine glass tags to a clearer, more transparent surface with visible background blur, restrained tint and fine edges. Keep readable labels and clear assigned/unassigned states over light and dark card art. Provide a legible fallback when backdrop blur is unsupported and respect reduced motion. Verify rendered appearance and interaction performance on desktop and touch layouts; do not equate emulation with physical-device verification. Status: production verified in r114-a1 within automated browser/cloud coverage; physical-device appearance and performance remain unverified.

  CARD-15/16 and DRAG-04/05 are locally in progress: [input and tag browser cases](../tests/ui/card-tags.spec.js) exercise native emulated touch at 320/390/1280px, actual mouse events in touch-capable contexts, centered fitted 200% radial tags, overflow, compatibility-click cancellation and dragging from the hover image. Frozen Remove is verified against a concurrent assignment change. [Radial presentation](../public/card-tag-wheel.js) shares per-card state with glass tags. Full legacy-suite migration, drag failure/cancellation coverage, glass artwork review, physical-device checks and cloud delivery remain outstanding.

  Further local evidence: [application card interactions](../tests/ui/card-actions.spec.js) retain ultrawide anchors, continuous hover tilt, fixed drag targets, deferred rendering, exact retry payloads, overflow/details access, mobile pinch and account isolation under the latest tags-only contract. A new wheel waits for an in-flight save and uses its settled assignments; cancellation and overflow actions recover the current source element after deferred rendering. [Visual prototypes](../tests/prototype) cover 54 Chromium/WebKit cases including touch 200%, font delay/failure, glass pointer light/press, viewport fit and all CARD-10 return cases. The combined card suites have 31 Chromium passes and 32 WebKit passes including enlarged touch drag. Six further cases pass in each engine: enlarged-artwork drag/cancel, outside tag-link frozen Remove, lost pointer capture, resize, second-finger pinch handoff and sign-out. Full application/live verification and artwork material review remain outstanding; none of these feature changes is deployed.

  Earlier local verification checkpoint: Full Chromium application coverage passed 124 cases plus the later touch-menu case separately; the current required WebKit selection passed 89 with one unsupported-stream skip, and all 54 visual prototypes passed. Public-art and forced no-blur comparisons were inspected; touch label backing was refined. Legacy application/live selectors now use the source menu and dedicated forms. [Revision evidence and limits](../tests/performance/CARD-TAG-REVISION.md) supersedes the earlier outstanding-local-check notes above. Production delivery and physical-device checks remain pending.

  Release preparation found a public fixture-host outage in both PR CI and the source build. Under DEPLOY-04/QUALITY-01, retain the exact existing three public smoke-test images and checksums in the repository, include only those named fixtures in the corresponding source archive, and continue rejecting unrelated photographs. Acceptance: builds use the same verified bytes without requesting the image host; archived source reproduces them and stays within the existing size bound. [Fixture/source regression](../recognition/tests/test_public_fixtures.py) passes locally; remote build verification is pending. This changes build inputs, not recognition models or accuracy claims.

## Deployment priority — completed; resume saved scanner/UI work

- **DEPLOY-05 (priority update):** Prioritize a separate frontend-only deployment workflow ahead of the remaining UI requirements. It must reuse the existing trusted main-branch release process and published backend compatibility checks, with fail-closed eligibility and a full-deployment fallback under DEPLOY-01–04. Preserve already completed work on other requirements; resume that backlog after the faster deployment route is verified. This supersedes the earlier decision to pause deployment-speed work behind UI/scanner implementation. Status: completed and production-verified; resume the preserved scanner/UI backlog. Evidence: [frontend deployment comparison](../tests/performance/FRONTEND-DEPLOYMENT.md).

## Documentation workflow

DEPLOY-01–05 delivery checkpoint: full bootstrap and the separate frontend path are production-verified. Explicit compatible frontend redeploy never overrides full-input or compatibility guards. Evidence: [use case and tests](USE-CASES.md#frontend-deployment), [actual timing and identity comparison](../tests/performance/FRONTEND-DEPLOYMENT.md). Scanner/UI work remains separate and unshipped until its own release evidence is recorded.

For each new request, update this ledger and the relevant use case before or alongside code. Retain corrections explicitly, add verifiable acceptance criteria, and attach test/release evidence when available. Link detailed design decisions in ARCHITECTURE.md and operational limits in OPERATIONS.md. Never silently drop queued work or report it as implemented.

CARD-10 / CARD-16 release repair: a late collection/tag refresh must keep the current source tile hidden while its enlarged artwork is open, then restore that exact printing and keyboard focus on dismissal. The deterministic delayed-tag regression failed on r107 source because general grid rendering bypassed save-only source reconciliation. Reconnect sources after every grid render; preserve the same fit assertions. LIVE-12 must use the current More tags / Card details route. Status: production verified in r114-a1, including LIVE-17/18 and the unchanged fit assertions. Evidence: [regression](../tests/ui/card-actions.spec.js), [original failed diagnosis](https://github.com/saintiago/magic-collection-keeper/actions/runs/34430152131) and [successful release evidence](../tests/performance/R114-RELEASE.md).

## Exclusive continuous-scan repair

- **SCAN-08:** Owner reports continuous scanning stalls after sliding in the next physical card, with `Wait until only one card is visible` and `Hold still` shown for The Arkenstone. Investigate both replacement/departure admission and single-card geometry; the screenshot alone does not establish the cause. Restore hands-free scanning of successive slid-in cards without requiring an empty tray between every card, while preserving stationary-card duplicate protection and actual multiple-card rejection. Add focused regression coverage for replacement and the pictured adventure-layout false rejection where reproducible. User explicitly requests only this fix due to low usage: pause all other implementation, minimize investigation/test scope to meaningful checks, and deploy the isolated repair through the existing eligible workflow. Status: requested, not fixed.

SCAN-08 supersedes the SCAN-04 requirement to observe an empty guide between every pair of cards. A stable, visually different card may rearm through fresh single-card geometry and cropped artwork comparison; brightness/contrast, partial glare, stale results, observation gaps and actual multiple-card frames must not rearm the outgoing card. Identical-looking copies still require observable departure. No geometry threshold or model is relaxed based on the screenshot; its full guide replays as single locally, while a tighter crop is ambiguous. Clear outdated geometry warnings when a fresh single-card result arrives. Status: locally verified; deployment pending. Eleven focused unit cases and the actual-geometry continuous-loop regression pass, including next-card replacement, stationary duplicate suppression, overlap and identical-copy departure. Both Chromium and WebKit replay the supplied full-guide screenshot as single; no geometry threshold was changed. LIVE-04 now verifies different-card replacement without a blank interval, stationary suppression, and explicit Add/reload persistence on the reserved profile.

## Artifact retention

SCAN-08 / DEPLOY-06 delivery checkpoint, September 11: **deployed and verified in r123-a2**. This supersedes the pending/requested statuses below. [Release evidence](../tests/performance/R123-RELEASE.md): LIVE-04 passed replacement without a blank interval, stationary suppression, explicit Add and reload persistence; all 12 desktop and 8 mobile live cases passed. Automatic cleanup removed both originating app artifacts after success. The committed CI changes required refreshed corresponding source; the original guard was preserved and now verifies 126 matching private/runtime/build files with recognition source version 18. Physical-device verification remains pending; all unrelated backlog stays paused.

- **DEPLOY-06:** Set explicit short retention for GitHub Actions artifacts and automatically remove disposable artifacts after a successfully verified deployment, preventing another storage-quota deployment block. Default disposable intermediate build/test artifacts to one-day retention; failed-run diagnostic artifacts may have a brief documented retention where needed for investigation. Audit actual consumers before cleanup: do not delete artifacts still referenced by active runs, publication, rollback or required source availability. Prefer durable release/source locations already in use rather than retaining redundant GitHub copies. Cleanup must be scoped to this repository's known artifact types/run IDs, use least privilege, and never delete owner data or durable release assets. Document exceptions and verify cleanup safety for success, failure and concurrent runs. This is explicitly authorized follow-up to the scanner deployment blockage; other feature backlog remains paused.

Status: locally verified with 137 unit tests, including five cleanup safety cases; main activation pending. Evidence: [cleanup policy tests](../tests/artifact-cleanup.test.js), [consumer audit and exceptions](OPERATIONS.md#github-artifact-retention). SCAN-08 remains merged and locally tested, not live: deployment attempt 2 still failed at artifact quota after the two explicitly approved archived recognition copies were deleted.

## Reopened physical scanner report

- **SCAN-09:** Owner tested deployed r123-a2 and reports that only the first card scans; subsequent cards do not scan when a card previously occupied the viewport. SCAN-08 automated release evidence did not establish physical acceptance and must not be described as resolving this report. Reopen the exclusive scanner scope: trace retained capture/replacement/geometry state after first recognition, reproduce realistic continuous card replacement and identify the blocking condition before another change. Preserve stationary duplicate protection, actual multiple-card rejection and explicit ownership confirmation. Record physical report versus automated evidence separately; all unrelated backlog remains paused to conserve usage. Status: reported on production, unresolved.

The owner physical failure after r123-a2 supersedes any claim that SCAN-08 automated evidence resolved their workflow. SCAN-09 causal diagnosis is complete: broad artwork correlations blocked distinct real cards despite stable single-card geometry. [Diagnosis and local verification](../tests/performance/SCAN10-REPAIR.md). SCAN-10 supplies the explicitly requested replacement policy; physical acceptance remains pending.

## Simplified consecutive recognition policy

- **SCAN-10:** Supersede the strict visual departure/replacement admission requirement for consecutive scans. Owner explicitly chooses to ignore consecutive recognitions of the same card instead of distinguishing consecutive identical physical copies. Once a capture resolves, suppress a consecutive match to the last accepted card identity; accept a different resolved card without requiring an empty viewport or strict visual-content change thresholds. A then A adds once; A then B adds both; A then B then A adds all three. Consecutive identical copies require manual quantity adjustment. Retain minimal usable single-card/stability capture handling, actual multiple-card rejection, bounded recognition requests and no success cue for unresolved or suppressed duplicates. Late parallel replies update their original capture and cannot add extra copies or reorder the accepted identity sequence. Preserve exact printing metadata and explicit ownership confirmation. Document identity comparison clearly and cover these sequences; this supersedes earlier requirements to support automatic consecutive identical physical copies and visual departure gating independently of names within this scope. Status: production verified within automated browser/cloud coverage in [r125-a2](../tests/performance/R125-RELEASE.md), including all 12 desktop and eight mobile live cases in exact-release verification 34597459303. Physical acceptance remains pending. [Implementation, tests and limits](../tests/performance/SCAN10-REPAIR.md). Other backlog remains paused.

SCAN-09 causal replay found broad low-resolution artwork correlations blocked distinct actual cards despite stable single-card geometry. Detail-comparison research is superseded by this explicit owner instruction; no such candidate was deployed.

SCAN-10 verification correction: r125-a2 is published and actual LIVE-04 passes consecutive suppression, A,B,A order, manual quantity, explicit Add and reload persistence. The unchanged shared LIVE-14 photo helper still expected two uploads of the same card to create two rows, so 11/12 desktop cases passed and mobile did not run. Update desktop/mobile LIVE-14 to require one row after the duplicate, then manually set three copies and verify one reviewed row, explicit Add and reload. Use the existing exact-release verification workflow against r125-a2; do not republish unchanged application code or classify the failed originating run as successful. Physical acceptance remains pending.

SCAN-10 final verification checkpoint: [exact-release run 34597459303](https://github.com/saintiago/magic-collection-keeper/actions/runs/34597459303) passed 12 desktop and eight mobile live cases on published r125-a2 after the shared LIVE-14 correction. Independent verification confirmed frontend/API/source/config/vendor identities and an empty reserved temporary profile. The originating main run remains failed and its two artifacts keep their normal one-day expiry; no success-cleanup claim applies. [Evidence and physical limitations](../tests/performance/R125-RELEASE.md).

## Physical acceptance and sustained scanning investigation

September 11: owner reports SCAN-10 scanning now works on their phone. This establishes acceptance of that tested sequence, not sustained thermal performance or all device conditions. They report the 50-card stop/restart issue and a very hot phone, and target thousands of cards over hours.

- **SCAN-11:** Owner confirms scanning works physically after SCAN-10, but camera stops after 50 accepted scans and immediately stops again on restart; phone becomes very hot. Investigate CPU/memory and propose improvements only, before further implementation. Distinguish deliberate session caps from crashes and measured device thermal evidence from code-derived hypotheses. Prioritize bounded resumable draft batches beyond 50, reduced idle inference/capture allocation, reusable low-resolution analysis buffers and on-demand high-resolution recognition. Preserve the accepted consecutive-identity rule without reintroducing strict visual replacement gates. Measure active/idle inference rate, frame allocations, long-session memory trend and responsiveness on representative devices before claiming thermal improvement. Status: investigation requested; physical sequence acceptance reported by owner; performance changes not implemented.

- **SCAN-12:** Design and evaluate continuous scanning for thousands of physical cards over hours in one session. No fixed session count such as 50 accepted cards or 150 attempts may force camera shutdown or restart. Internal bounded request/commit batches remain acceptable and must roll over transparently. Persist pending captures incrementally with retry idempotency and explicit later ownership confirmation, preserving the last accepted identity across internal batch rollover.

- **SCAN-13:** Session memory and active work must remain bounded independently of total scanned count: retain only a bounded recent view and in-flight image buffers, release image resources after use, and retrieve older persisted items on demand. Apply bounded backpressure and recoverable persistence/network states without silently losing captures. Review must accommodate the accumulated session without loading all images/forms at once. Preserve capture order and reload recovery.

- **SCAN-14:** Validate sustained performance with a staged soak test targeting at least 2,000 accepted captures over at least two hours as an initial engineering benchmark for the owner's 'thousands over hours' goal, not a promised throughput or maximum. Measure memory plateau, inference/capture rate, latency drift, responsiveness, duplicate suppression, persistence/recovery and device heat/battery observations. Physical-phone validation is required before claiming acceptable thermal behavior; replay validates only its stated scope. Current request extends investigation/proposal scope; implementation remains pending.

Investigation checkpoint: static inspection of the exact r125 public runtime identifies retained full-batch counters as the immediate restart cause and concrete continuous inference/allocation/persistence costs. [Findings and prioritized proposals](../tests/performance/SCAN11-INVESTIGATION.md). Investigation complete within code-inspection scope; proposed performance implementation, soak measurements and physical thermal acceptance remain pending. No runtime change, broad test or deployment was performed.

## Import confirmation optimization — plan, not implemented

- **IMPORT-05:** After the backend confirms Add with a durable receipt, remove only that confirmed pending draft/cards from the Import view without awaiting or initiating a full collection refresh for this page. Keep unrelated pending drafts, ordering and scroll intact. Mark collection-derived caches stale and refresh lazily when a consumer needs them; preserve receipt-confirmed Recent activity. Pending entries must not disappear before successful confirmation. Unknown network outcome retains a retry using the same operation identity; no duplicate ownership.
- **IMPORT-06:** Measure Add request/backend preparation/commit time separately from post-response UI work. Optimize backend reads to affected printing/finish/condition/source/tag records where safe; do not remove quantity, source provenance, concurrency or idempotency checks merely because the Import page does not display the collection. Reuse trusted cached printing data where valid, batch remaining lookups, and avoid repeated full inventory scans. Status: planned; no measured speedup claimed.
- **IMPORT-07:** Verify successful Add removes only confirmed pending content without a full collection request; failure preserves review state; lost-response retry is idempotent; subsequent collection/Home navigation refreshes stale data; concurrent edits retain version protection; 50-row and larger chunked-session confirmations remain bounded. Use isolated profiles and report actual stage timings before/after.

### Implementation sequence for IMPORT-05–07

1. Instrument existing Add timing boundaries with aggregate durations only, separating response/commit from collection refresh/render; avoid logging private card data.
2. Remove the unconditional awaited collection refresh from the Import success callback. Reconcile the receipt into pending state and Recent activity, invalidate collection-derived caches, and retain lazy refresh on consumer navigation. Preserve relevant pending-view context.
3. Run focused success/failure/lost-response/navigation checks; measure this small UI change independently before broad backend optimization.
4. Profile backend preparation and atomic commit. Replace whole-collection reads with bounded affected-record access where compatible with source/ownership semantics; batch catalog lookups. Retain transaction/version/receipt protections.
5. Measure realistic isolated 1/10/50-row confirmations and sustained-session batches, then validate through the existing deployment workflow when implementation is authorized. This request authorizes planning; do not start implementation solely from this plan.

Traceability: planned UC-IMPORT-CONFIRMATION below in USE-CASES.md; planned receipt reconciliation and lazy collection refresh in ARCHITECTURE.md. Current callback remains `public/app.js` Import `onAdded` with unconditional `await refresh()`; this planning update changes no runtime behavior. SCAN-11–14 and IMPORT-05–10 remain unimplemented proposals.

### Backend confirmation requirements — planned, not implemented

- **IMPORT-08:** Add confirmation must avoid loading the entire owned collection to prepare a bounded pending import. Read only affected printing/finish/condition ownership records, applicable prior source contributions and tag/location records. Preserve quantity bounds, concurrent-edit protection, loose copies and permanent source-import idempotency. Verify read scope does not grow with unrelated inventory size.

- **IMPORT-09:** Reuse trusted printing metadata and validation prepared during draft staging or printing/finish edits. Avoid redundant printing lookups during confirmation when the validated data remains applicable. Revalidate changed or missing dependencies and never trust client-supplied validation claims. Preserve exact-printing, language, supported finish and paper-card rules; measure lookup count before/after.

- **IMPORT-10:** Prepare reusable import data during staging/editing instead of repeating the full general deck-import preparation at Add. Keep ownership-sensitive calculations based on current affected records at commit time, with version checks or equivalent concurrency protection. Add atomically applies ownership/tag/location/source changes, records an idempotency receipt and clears pending state; no ownership is created during staging. Preserve existing source-refresh semantics and avoid a separate implementation that drifts from shared domain rules. Target a small confirmation path, not merely shifting repeated expensive work onto every scan. Status: requested/planned, not implemented.

Planned acceptance evidence: compare affected-record read scope as unrelated inventory grows; count printing lookups for unchanged, edited and missing validated metadata; exercise source refresh, retained loose copies, concurrent ownership edits, transaction failure and lost-response retry. Staging must create no ownership, and confirmation must atomically apply changes, write its receipt and clear only confirmed pending state. Measure staging/editing as well as Add to reject a design that moves repeated expensive work onto each scan. Evidence is not yet produced. Traceability: UC-IMPORT-BACKEND in USE-CASES.md and the planned backend section in ARCHITECTURE.md. These extend IMPORT-05–07 and do not authorize implementation.

## App-wide bounded collection access — planned, not implemented

The owner requires filtered/limited access throughout the app for inventories of at least 30,000 cards. This extends the Import and sustained-scanner plans to every ordinary collection consumer; no runtime change or migration is authorized by this requirements update.

- **SCALE-01:** No ordinary page or workflow may load the entire owned collection. All collection/list endpoints enforce a finite server-side maximum page size; views request only relevant filters and a bounded page, including the unfiltered collection view. Target inventories of at least 30,000 cards. An unfiltered view means a paginated view, never an unlimited response. Status: requested/planned, not implemented.

- **SCALE-02:** Execute search, filtering and sorting on the backend before pagination, with stable ordering and deterministic tie-breaking/cursors. Preserve navigation filters, scroll and focus with bounded account-isolated state. Load subsequent pages explicitly or incrementally with bounded rendered/cached items; infinite scrolling must not accumulate the entire inventory in memory.

- **SCALE-03:** Home, Recent, autocomplete ownership indicators, card details, tag/deck views, pending imports and mutation refreshes use bounded specific queries or batched identity lookups. Totals/type summaries/Commander-view eligibility use backend aggregate or existence queries across the matching set, never client-side full collection downloads or inference from only the current page. Backend queries must avoid scanning/materializing all unrelated inventory merely to return a limited response; evaluate indexes and affected-record access.

- **SCALE-04:** Replace broad collection refreshes after mutations with affected-item updates/invalidation and bounded visible-query refreshes. Keep ownership quantities, source provenance, tag assignments and account isolation correct. Explicit full exports, if supported, use a deliberate paged/streamed export workflow rather than normal UI loading.

- **SCALE-05:** Audit existing full-list callers and create traceable migration coverage. Validate an isolated 30,000-card inventory: bounded responses, browser memory and rendering; correct whole-result filtering/sorting/counts; stable pagination; concurrent changes; navigation/reload; no cross-account cache leakage. Measure query/read costs and latency as inventory grows. Do not claim scalability from adding a frontend slice or API limit after reading the whole inventory. This request records requirements; no implementation/deployment is implied.

Traceability: planned UC-SCALE-ACCESS and UC-SCALE-ACCEPTANCE in USE-CASES.md, and planned bounded collection access in ARCHITECTURE.md. The caller audit, query/index design and isolated 30,000-card acceptance fixture remain to be implemented and measured. SCAN-11–14 and IMPORT-05–10 remain planned alongside SCALE-01–05; no scalability or delivery claim is made.

## Catalogue and private membership access — accepted strategy, not implemented

- **DATA-01:** Use Scryfall for general catalogue search and canonical card/printing metadata; do not rebuild its general search unnecessarily. Enrich each bounded search page with a bounded batch lookup of the current owner's exact-printing quantities, tags and locations. Preserve Oracle versus printing identity, language/finish/condition distinctions and separate pending state. No full-collection fetch to annotate results.

- **DATA-02:** Owned, tag/deck and pending-import views start from indexed owner-scoped membership records, with server-side filtering/sorting and pagination. Enrich only the resulting page with shared cached printing metadata. Scryfall cannot determine private ownership or memberships; never use a page of global search results as a substitute for complete owned-result filtering.

- **DATA-03:** Retain only the local searchable/sortable card metadata required by supported owned-view queries, such as normalized name and card type, with a defined refresh/version policy. This is a query projection of canonical metadata, not ownership or a replacement general catalogue. Design membership/index access around documented view combinations; do not fetch all matching cards to sort before paging.

- **DATA-04:** Keep shared printing cache separate from owner quantities, tag/location assignments, pending captures and source provenance. Preserve the shared Scryfall limiter, cache and cooldown; hydrate bounded cache misses through existing provider policy. Aggregate quantities correctly across owned variants without conflating printing and Oracle identity. Verify catalogue enrichment, private-list completeness, cold-cache behavior, rename/metadata refresh, pagination and account isolation at the 30,000-card target. Status: accepted strategy, not implemented.

Traceability: planned UC-DATA-CATALOGUE and UC-DATA-PRIVATE in USE-CASES.md and accepted data-access strategy in ARCHITECTURE.md. Define supported view/query combinations and projection refresh/version behavior before implementation; validate bounded owner enrichment, complete private results, metadata/cache refresh and account isolation under SCALE-05. This supplements SCALE-01–05 and IMPORT-05–10; no implementation, migration, provider-policy change or deployment is included.

### Progressive owner enrichment — accepted strategy, not implemented

- **DATA-05:** Where catalogue result membership/order comes from Scryfall independently of ownership, render the bounded Scryfall page as soon as available without awaiting owner database enrichment. Fetch ownership, quantities, tags and locations in a bounded batch for that page and update cards in place as results arrive. Preserve order, layout, focus, hover/zoom/drag state; deduplicate redundant requests.

- **DATA-06:** Unresolved ownership enrichment is an explicit loading/unknown state, never zero quantity or unowned. Failed enrichment leaves catalogue results usable with targeted retry; ownership-dependent actions wait for authoritative data or use their existing validating mutation path. Do not overwrite newer user mutations with stale enrichment responses. Account changes/navigation/search changes invalidate late replies using account/query generation and exact-printing identity.

- **DATA-07:** Progressive enrichment must not substitute an unfiltered Scryfall page for owned/tag/deck/pending-filtered query results or expose wrong result counts/order when ownership determines membership. Those views remain driven by indexed private queries under DATA-02, though metadata/images may load progressively once membership is known. Test delayed/failed/out-of-order enrichment, pagination, rapid searches, sign-out/account switching, active card interactions and concurrent tag/quantity edits. Status: accepted strategy, not implemented.

Traceability: planned UC-DATA-PROGRESSIVE in USE-CASES.md and the progressive enrichment subsection in ARCHITECTURE.md. This extends DATA-01–04 without changing private membership/query semantics, SCALE bounds or existing validated mutations. Tests, implementation and performance evidence remain pending.

## Authorized continuous scanner implementation — in progress

The owner now explicitly authorizes implementation and tested deployment of prioritized items 1 and 2: continuous scanning beyond 50 with incremental durable pending batches and bounded history/recovery (SCAN-05/12–13), plus reduced sustained capture/inference/allocation/idle work (SCAN-11–14). Preserve SCAN-10 A,A suppression and A,B,A acceptance across internal rollover and resume. Bounded request/transaction chunks remain; accepted-card/attempt session counters must not shut down normal capture. Storage/network pressure must be bounded, visible and recoverable without losing confirmed captures or creating ownership. Review remains explicit and loads bounded historical content. Record before/after resource measurements and a meaningful sustained soak; the initial >=2,000 captures over >=2 hours target must be labeled with actual completed scope. Physical heat improvement requires owner-device validation.

Status: implementation in progress, not locally verified or deployed. Only necessary scanner/session storage, recovery and bounded review changes are authorized. IMPORT-05–10, SCALE-01–05, DATA-01–07 and unrelated feature plans remain unimplemented and are not independently included. Existing data, permanent receipt/source idempotency, source availability, separate reserved test profiles and the normal deployment workflow remain required.
