# Baseline reconciliation — September 12, 2026

HARNESS-15/16 step 1. Original working checkout `0680a01` was 52 commits behind current `origin/main` (`e0815c3d8513f0af2d9bc3af89bf0a138f2db394`). Its uncommitted charter/harness requests and other working files are preserved. The original requirements and current-main requirements were backed up under `%LOCALAPPDATA%/DevelopmentHarness/baseline-2026-09-12`. Current application requirements were reconciled with the newly approved product/harness prefix; prior acceptance text and superseded history remain in the authoritative file. An isolated delivery worktree starts from current main at `E:/projects/magic-keeper-delivery`, branch `codex/harness-first-delivery`.

## Current release versus old reports

Direct read-only checks of the published HTML and version metadata returned **r127-a1 / 0.1.0+deploy.127.1**, application/API commit `5afdb17994d91d475cebe4f84972cb6a3827844b`. GitHub [main run 34632270262](https://github.com/saintiago/magic-collection-keeper/actions/runs/34632270262) succeeded. The later main commit changes release documentation, not app runtime.

- DEPLOY-01–05's frontend separation already exists and has documented production verification. The current classifier permits a conservative presentation allowlist, rejects unknown/full inputs and preserves main as the entry point. Rebuilding a frontend-only pipeline would duplicate delivered work. [Evidence](https://github.com/saintiago/magic-collection-keeper/blob/e0815c3d8513f0af2d9bc3af89bf0a138f2db394/tests/performance/FRONTEND-DEPLOYMENT.md).
- DEPLOY-06 is deployed. The trusted-main cleanup workflow limits deletion to verified exact-run disposable handoffs; source/recognition cross-run artifacts have explicit exceptions. [Cleanup run 34634495784](https://github.com/saintiago/magic-collection-keeper/actions/runs/34634495784) succeeded after r127.
- SCAN-08/09's strict replacement-policy failure was superseded by the explicit SCAN-10 identity-suppression policy. The owner accepted that sequence physically. Later SCAN-11–14 work delivered persistent batching and reduced analysis work, with automated release and a two-hour synthetic persistence soak. Actual phone heat/battery improvement remains unverified and cannot be resolved by pretending to perform physical checks. [r127 scope](https://github.com/saintiago/magic-collection-keeper/blob/e0815c3d8513f0af2d9bc3af89bf0a138f2db394/tests/performance/R127-RELEASE.md).
- Several old card/Recent and deployment statuses are superseded by explicit release checkpoints. The normalized fields identify those checkpoints; unknown or partially covered acceptance remains visible rather than silently completed.

## Bounded first-fix selection

Select **IMPORT-05**, with its IMPORT-07 acceptance checks and only the relevant IMPORT-06 timing boundary. Current `public/app.js` Import `onAdded` records receipt-confirmed Recent cards, invalidates the cache, then unconditionally awaits `refresh()`. That fetches the entire collection and tag registry even while the user remains on Import. The saved requirement explicitly asks to remove that dependency while preserving receipt semantics and lazy refresh for consumers.

Rationale: higher-priority scanner and deployment repair streams have explicit delivered/superseded evidence; remaining physical thermal validation needs real device evidence, not another speculative patch. Import confirmation has a concrete current-code trigger, measurable request behavior, existing acceptance and a bounded frontend/cache/navigation scope. Wider backend ingestion/30,000-card work and new deck inspiration remain separate. This is the delegated first-delivery selection under HARNESS-15, not a claim that all backlog priorities are settled.

Before implementation, validate hosted Node 24/build/browser compatibility, isolate data/test profiles, enforce exact-source acceptance/authorization/budget gates, and establish an independent review role. The chosen change must show successful receipt removes only confirmed pending content without collection I/O, failures retain drafts, lost-response retries remain idempotent, and next collection/Home consumer refreshes stale data. Existing main release classification remains authoritative; `public/app.js` is intentionally a full-release input. Do not widen its frontend allowlist to make this change appear faster.

## CI/CD observed baseline and candidates

Run 34632270262 took about 23m36s from creation to successful deployment-job completion. The test job took 14m53s; deployment 8m24s. Observed steps: Chromium 6m07s, WebKit 4m30s, visual checks 1m47s, protected live verification 5m41s, browser installation 55s in test and 78s in deploy; recognition asset preparation 32s. These are one-run measurements, not a stable performance baseline. Raw job metadata is retained in the local baseline evidence directory.

Assess, rather than immediately apply: browser-install reuse/caching, duplicate work and safe test scheduling. Any parallelism must respect isolated profiles, publication locks and actual worker constraints. Node dependency caching and verified frontend vendor reuse already exist. Production rollback has guarded local tests; it was not deliberately exercised in production. Preserve permissions, exact artifact/source provenance, quota/retention exceptions, live checks and failure recovery. An independent reviewer still needs to validate workflow behavior and findings. No CI optimization is adopted merely from these observations; no-action/defer are valid outcomes.

## Hosted readiness and independent review checkpoint

Node 24.14.1, npm 10.9.8, Python 3.12.12, app build and 142 unit tests passed in cycle 5afb1521-2ef9-43c5-97bb-70ae792025c6. Browser download initially failed because the Playwright CDN redirects Chrome to storage.googleapis.com. A corrected restricted allowlist and disposable host dependencies passed Chromium 153.0.8010.12 and WebKit 26.6 launch/click/DOM smoke in cycle 5cf035cb-fb4e-4809-9999-25d991fb1bc7. Both used the committed archive SHA-256 92cac4868a53b06c777b90c7427e0c877bacfc02243cf06fedc845c09be4a076. The source-to-commit binding is controller-attested git archive; no .git or credentials are sent. Browser smoke is runtime readiness, not app E2E or physical-camera proof. The controller implementation gate passed using actual saved command outputs; provider command exit/duration fields remain null, with shell status text retained.

Independent CI review c2ce554e-da74-43f8-b3ae-9f10c985a6dd accepted IMPORT-05/full-release selection and deferred caching/scheduling changes. It raised publication-order/source concerns because prepare-release.mjs, release.mjs and verify-release-backend.mjs were omitted from its input. Those scripts contain the newer-release guard and authenticated digest-verified source retrieval; findings require independent re-review with this missing evidence before release. This exposed a reviewer context-coverage weakness, not grounds to rewrite working CI.

