# Use cases and E2E coverage

[Owner requirements, acceptance criteria and queued requests](REQUIREMENTS.md).

**UC-SCAN-START:** Home Scan remains disabled and marked busy while the verified account and saved captures load. Once initialized, the first tap opens the scanner. `tests/ui/phone-review.spec.js` delays capture recovery deterministically; LIVE-13 starts at Home immediately after sign-in against the deployed app. No test changes that origin to bypass startup.

## Loading, device cache and release identity

UC-19: every displayed tag kind links to its exact-ID collection filter from cards, details, management, assignment labels and source headings. Navigation clears conflicting search/color/set/finish filters, keeps location-copy counting, closes dialogs without saving unsaved edits, and focuses the library heading. Active tag/clear, keyboard Enter, browser Back, reload/cache, same-label different IDs, rename and deletion are covered. Rename/remove/checkbox controls remain separate. Evidence: `tests/tag-navigation.test.js`, `tests/ui/tag-navigation.spec.js`, existing assignment-control tests and deployed LIVE-03.

UC-18: location-filtered totals count assigned physical copies, with distinct entries separately; quantity sorting and per-card labels use the selected location while top statistics retain whole-collection ownership. Search narrows the displayed copies out of the full location total. Source cards include commander/main quantities and explicit pending printing references, excluding retained loose copies from current source size. Cached locations remain filterable during refresh. Regression evidence: `tests/collection-counts.test.js`, `tests/ui/deck-counts.spec.js`, `tests/live/tags.spec.js` (LIVE-03). The 88-entry fixture contains 99 assigned copies plus one pending source copy, repeated lands shared with another deck, and separate finish rows.

| ID      | Observable outcome                                                                                                                                                                                                                                                                                                | Evidence                                                                                  |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| UC-16   | Local fallback; visible deployment version; About commit/time/checks link; mobile close/Escape; distinct retries; old HTML retains immutable metadata; refuse regressing deployments                                                                                                                              | tests/release.test.js, tests/ui/loading.spec.js, LIVE-05                                  |
| UC-17   | Cold placeholders; failure/retry without false empty; cards before slow tags; 2,870-entry warm snapshot with timestamp/updating; failure retains cards; successful empty reconciliation; verified account isolation/logout clearing; corrupt/quota cache fallback; stale read/mutation/account response rejection | tests/collection-loader.test.js, tests/ui/loading.spec.js, tests/ui/auth.spec.js, LIVE-01 |
| LIVE-05 | Published HTML and immutable metadata match the GitHub deployment; loaded footer and commit agree with protected API version headers; no owner mutations                                                                                                                                                          | tests/live/release.spec.js                                                                |

LIVE-01 waits for a real test-account snapshot, reloads while the next real collection request is delayed, verifies cached totals/updating, then releases and reconciles the cloud response. Mocked large fixtures prove UI behavior, not a particular cause for a user's network delay. Totals compute synchronously; loading/updating are the asynchronous labels.

All currently visible interactive controls are grouped below. The names identify executable tests in `tests/ui`; production behavior is additionally covered by `tests/live/deployment.spec.js`. UI tests use controlled API fixtures for deterministic error/edge states. They are not substituted for the live AWS/Scryfall test.

| ID      | User interaction / observable outcome                                                                                                                                                                                           | E2E evidence                                                                                                     |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| UC-01   | Sign in, rejected credentials, temporary-password challenge, sign out, expired session refresh, revoked refresh returning to login                                                                                              | `auth.spec.js`: both UC-01 tests; LIVE-01 real Cognito sign-in/out                                               |
| UC-02   | Empty collection; My collection/Discover/Add/first-card navigation; summary counts; name/type/set/language/condition text search                                                                                                | `app.spec.js` collection flow; `interactions.spec.js` UC-02/03 and UC-09                                         |
| UC-03   | Color, set and finish filters; colorless; no matching cards and Clear filters; name/recent/quantity sort; Update collection success and failure preserving current cards                                                        | `interactions.spec.js` UC-02/03; `app.spec.js` collection flow; LIVE-01 refresh/reload                           |
| UC-04   | Blank search validation, query submit, loading, no results, Scryfall failure, example query, Load more, navigate away during search                                                                                             | `interactions.spec.js` UC-04; `scan-review.spec.js` UC-04 loading test                                           |
| UC-05   | Open printing; image/missing-image view; flip double-faced card; external Scryfall link target; choose quantity/finish/condition; language read-only; invalid quantity; add, retry failure; close via button/Escape             | `interactions.spec.js` UC-04 and UC-05; `app.spec.js` collection flow; LIVE-01 canonical real printing add       |
| UC-06   | Edit quantity; cancel removal, confirm removal, remove failure; changed data visible in collection and after reload                                                                                                             | `app.spec.js` collection flow; `interactions.spec.js` UC-05; LIVE-01 reload/edit/delete                          |
| UC-07   | Paste list, empty/oversized/invalid line errors, Preview matches, unresolved lookup, edit lookup and Find, choose candidate or leave out, edit quantity/finish/condition, ownership checkbox, save/partial failure/retry, close | `app.spec.js` import ownership test; `interactions.spec.js` UC-07; `scan-review.spec.js` UC-07                   |
| UC-08   | Start camera and permission/fallback errors; photo upload; recognition failure/unclear reading; final review correction, ownership/save and discard                                                                             | `app.spec.js` permission and actual recognition tests; `interactions.spec.js` UC-08; `scan-review.spec.js` UC-08 |
| UC-09   | Desktop and 390px mobile navigation, collection controls and scan modal remain reachable without horizontal overflow                                                                                                            | `interactions.spec.js` UC-09 plus hands-on desktop inspection                                                    |
| LIVE-01 | Anonymous request rejected, real sign-in, real Scryfall search/image, cloud add/edit/delete, reload persistence, second owner's collection isolated, foreign entry update rejected, sign-out                                    | `tests/live/deployment.spec.js`; runs after each deployment                                                      |

`npm test` also verifies identity/printing/inventory separation, merging and quantity bounds, durable SQLite reopening, operation retry idempotency, application owner propagation, parser behavior, frame differences, cache bypass and 429 cooldown.

## Manual acceptance workflow

`tests/live/capture-import.spec.js` (LIVE-02) exercises real pasted-text resolution and durable reviewed ownership. LIVE-14 exercises the deployed photo-upload/model/review/save path using public catalog artwork. Permission timeout and invalid-image upload errors are covered in `scan-review.spec.js`.

Use the dedicated `keeper-e2e` profile, never the owner's `saint282` account. Open the CloudFront URL, sign in, search an exact printing, inspect its image and details, add a test quantity, return to the collection, refresh, reload, edit and delete. Try the scan screen, permission/fallback flow and a pasted import. End with an empty test collection and sign out. The separate `keeper-isolation` identity proves that another owner's row ID cannot be edited. No test invitation emails are sent.

Automated camera tests use a synthetic browser MediaStream to verify stability, duplicate suppression and track shutdown. The recognition test runs the actual shipped ONNX models against frozen public card artwork. These prove different things: neither establishes real-world accuracy on sleeved/foil cards or a physical phone camera. A real phone on a desk, rear camera permissions, lighting/glare, and consecutive identical cards still need a hands-on hardware acceptance run. This limitation is visible rather than hidden behind mocked recognition.

## Tag and deck-source use cases

UC-20: official precon imports retain a distinct Wizards source ID/URL and stable deck-location tag. Repeating imports adds zero copies; a source URL correction or tag rename does not add ownership. Copies shared with a Moxfield source aggregate without removing either allocation or loose inventory. Invalid provider/URL/namespace and unsupported finish fail before creating the source. Evidence: `tests/tags.test.js`, official provenance link in `tests/ui/tags.spec.js`, and LIVE-03's isolated Wizards fixture with cloud repeat-import, finish, source/tag provenance and reload checks. Official product contents are separately verified against the publisher's decklist and printing catalog before an authorized owner import.

| ID    | User interaction / observable outcome                                                                                                                                                | Evidence                                                                                               |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| UC-10 | Create typed locations, roles and categories; rename without changing identity; reject deletion while referenced; safely delete unused tags; close dialog and retain editable errors | `tests/ui/tags.spec.js`, `tests/tags.test.js`, LIVE-03                                                 |
| UC-11 | Open card tags; add/remove locations and copy quantities; classify cards; create tags in context; filter collection; icons and quantities in cards                                   | `tests/ui/tags.spec.js`, LIVE-03; contextual creation is also checked hands-on                         |
| UC-12 | Assign 3 with 2 owned, lower to 1, keep assignments, reload, clear shortfall by allocation removal or higher owned quantity; no blocking modal                                       | `tests/tags.test.js`, `tests/ui/tags.spec.js`, LIVE-03                                                 |
| UC-13 | Import two sources sharing a printing, retain loose inventory, repeat with zero additions, revise without deleting ownership; browse source provenance and excluded sections         | `tests/tags.test.js`, LIVE-03; actual Commander folder import is a separate owner-authorized operation |

LIVE-03 uses only `keeper-tags`, a third isolated profile that retains two clearly named synthetic Moxfield fixtures and one clearly named synthetic Wizards fixture to prove idempotency between deployments. The original two profiles remain separate and empty after their tests. The synthetic fixture contents are not evidence of an actual inspected deck, even when the provenance URL points to an official page. Actual imports must use verified product definitions or observed source IDs and exports. Tests and hands-on browser checks never use the owner's credentials.

## Coverage boundaries

Tests cover each visible control and its meaningful state transitions through representative inputs, not every possible value combination. External Scryfall links are checked for correct destinations rather than exercising Scryfall's own UI. Cognito's new-password screen is tested with a controlled challenge; no automated test changes the owner's password. Camera timeout, hardware-specific media errors, background tab lifecycle and every card layout are not exhaustively browser-tested. Missing/denied camera, recognition load error, unclear result, synthetic stream and real recognition are covered. Add new use cases and tests together when controls or adapters change.

## Hands-free scanner coverage

- **UC-ONE-CARD:** Require one stable visible geometry region before capture or photo recognition. Multiple/ambiguous/overlapping cards quietly wait without cues or copies; overlap cannot rearm the outgoing card. Empty departure and a stable identical card can add a subsequent copy. `tests/card-presence.test.js`, `tests/ui/one-card.spec.js`, actual Chromium/WebKit geometry replay and public backend regression in `tests/performance/SCANNER-GUARDS.md`.
- **UC-OVERLAY:** Map actual detected source regions through object-fit/clipped guide scaling; show only real stage activity and brief identity; reject stale capture events after departure/navigation, resize safely, disable pulsing for reduced motion and dispose animation. `tests/card-presence.test.js`, `tests/ui/one-card.spec.js`, actual worker draw timing in `tests/ui/scanner-model.spec.js`. Remote HTTP has no internal OCR progress stream, so it displays only general backend activity.
- **UC-INDEPENDENT:** Independent Nova Pro image proposal runs alongside existing Nova Lite OCR corroboration; no candidate priming, strict single-card/uncertainty and exact catalog validation, bounded one in flight/50 per session. First valid result and late supported alternatives share one capture without duplicate ownership/cues or lost edits. Preserve nested disagreement and provider failures; cancel late results. `tests/independent-recognition.test.js`, `tests/recognition-race.test.js`, `recognition/tests/test_independent_identity.py`, `recognition/tests/test_transport.py`, shared Import late-edit browser tests.

| ID      | Observable outcome                                                                                                                                                                                                                                                                                                                                                            | Evidence                                                                                                                                                                                         |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| UC-14   | Initial activation, continuous stable capture, stationary suppression, identical cards after departure, failed reads excluded from copies/forms; separate optional candidates; uncertainty/error recovery, once-per-attempt distinct cues, audio readiness/test tone/mute, Back/Escape, cancellation during recognition, background shutdown, real guide crop and recognition | `tests/scanner.test.js`, `tests/ui/scanner.spec.js`, `tests/ui/scanner-model.spec.js`, `tests/ui/scan-wheel-photo.spec.js`, LIVE-13, LIVE-04, `tests/mobile-live/scanner-errors.spec.js` LIVE-13 |
| UC-15   | FIFO insertion with newest at bottom/selected; smoothly scrolling native wheel, touch swipe and snapping, newest bottom alignment without trailing gap; older selection centering, keyboard/reduced motion, current-only +/− and removal, bounded quantity and current selection, portrait/desktop sizing                                                                     | `tests/ui/scanner.spec.js`, `tests/ui/scanner-model.spec.js`                                                                                                                                     |
| LIVE-04 | Generated camera stream through deployed real recognition and actual Scryfall lookup; identical consecutive copies; wheel quantity adjustment; final ownership save/reload and cleanup in dedicated test profile                                                                                                                                                              | `tests/live/scanner.spec.js`                                                                                                                                                                     |

The camera fixtures draw synthetic targets into a real browser MediaStream. Touch tests dispatch browser touch gestures; audio tests observe generated oscillator pitches. These verify browser logic and audio scheduling, not a physical phone lens, actual speaker audibility, or iOS-specific inertia. Physical identical-card transitions in varied lighting still need device acceptance. No owner account is used for testing.

## Saved import coverage

| ID      | Observable outcome                                                                                                                                                                                                                                 | Evidence                                                      |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| UC-21   | Dedicated Import navigation, persistent 100-card/100-line drafts, quantity/delete/printing/finish/tag review, reload, save failure/retry, unavailable-tag repair, Clear, mobile layout and Back/Forward; pending entries excluded from owned views | `tests/import-drafts.test.js`, `tests/ui/import-page.spec.js` |
| UC-22   | Atomic Add, durable replay receipts and source idempotency; preserved loose copies/manual assignments and original lines; Clear/Add race rollback, stale revisions and database restart                                                            | `tests/import-drafts.test.js`, `tests/ui/import-page.spec.js` |
| UC-23   | Public URL validation, SSRF/redirect rejection, credential-free provider requests, denial/timeout/throttle/size errors; canonical batch cache and rate limits                                                                                      | `tests/import-drafts.test.js`, LIVE-06                        |
| LIVE-06 | Actual JWT/Dynamo draft edits and reload persistence in keeper-import; ordinary views unchanged, reserved-tag and cross-account rejection; actual AWS-to-Moxfield request with honest access-result reporting                                      | `tests/live/import-page.spec.js`                              |

The persistent live fixture is explicitly synthetic, seeded only by an administrator script into the reserved test account. It proves cloud staging and review independently of Moxfield access. Provider denial is an expected reported limitation, never a successful URL-import claim. Local fixture success and synthetic camera tests do not prove upstream access or physical hardware accuracy.

## Discovery coverage

**UC-32 / LIVE-10:** Default Home shows search/Scan/Import/Tags and real recent card/deck/tag shortcuts in the phone viewport, with full inventory behind View collection. Card opens and tag visits preserve ordering/deduplication after reload; decks are distinguished by type/kind, labels are current and escaped, and account switches/sign-out cannot expose another history. Loading/failure remains honest, pending imports never count as ownership, and Clear activity is account-specific. Full collection/tag filters, Back/Forward, pasted/URL import and scanner remain available. `tests/home.test.js` and the four `tests/ui/home.spec.js` cases cover storage/bounds/rendering/routes/account/mobile behavior; CI runs Home in Chromium and WebKit. Existing collection tests explicitly use `#collection`. LIVE-10 uses keeper-tags' real synthetic-fixture inventory and deck links, creates/deletes one temporary role to verify tag activity, reloads, checks unchanged ownership and restores original tag IDs. Engine/emulation checks do not prove physical phone-camera or keyboard behavior.

**UC-31:** Autocomplete and recent card rows use compact inline ownership icons with accessible Owned/Not owned/loading/unavailable/saved labels and no quantities. A translated subtitle appears only for a match unexplained by the normalized English canonical or face names; accent/case normalization, overlapping Nephilim/French names, English faces, translated-only Relámpago and Fuego, and unchanged rank are covered. Recent cards omit historical aliases. Evidence: `tests/discovery.test.js`, `tests/search-ownership.test.js`, `tests/suggestion-view.test.js`, UC-31 in the Chromium/WebKit discovery suite, LIVE-08's actual owned icon and LIVE-09's actual Nephilim/translated-only/touch checks. Mobile layout and direct touch selection remain covered; WebKit emulation does not prove physical iPhone keyboard behavior.

**UC-30:** Empty focused shared search shows at most ten deduplicated recent committed queries and selected cards in recency order; typing restores ordinary suggestions. Recent card touch/keyboard selection opens common detail with current owned indicators; recent query selection runs unified search. Search result card choices also enter history. Reload and same-account sign-in preserve local history; verified account changes and sign-out clear visible private state, and clearing one account cannot erase another's history. Read/write/removal failures remain explicit and searching stays usable. `tests/recent-searches.test.js`, the three UC-30 cases in `tests/ui/discovery.spec.js`, and the UC-17/30 account-switch case in `tests/ui/loading.spec.js` cover bounds, deduplication, ordering, unfinished typing, storage failures, mobile clearing and isolation. LIVE-09 verifies actual deployed WebKit history persistence, recent card/query taps and clearing using only keeper-e2e, with unchanged ownership.

**UC-29:** One search field has identical semantics in My collection and All cards, retains text between views, and preserves independent collection filters. Owned indicators derive presence across multiple printing/language/finish rows without inventory requests per keystroke; quantities are not rendered in suggestions. Quantity changes update badges; first-load and saved-snapshot failures remain distinct; verified account changes and sign-out clear private ownership. `tests/search-ownership.test.js`, `tests/ui/discovery.spec.js`, `tests/ui/loading.spec.js`, LIVE-08 and LIVE-09 cover these paths. LIVE-08 starts shared search from the owned collection and checks the canonical suggestion's inline accessible Owned icon against actual inventory before tag editing. LIVE-09 taps unowned suggestions from both views using WebKit.

**UC-28 / LIVE-09:** Native Chromium and WebKit taps select once without depending on a synthetic mouse click, including a translated alias. A native-tap regression injects input blur during touchstart to cover keyboard dismissal and delays resolution to verify visible progress. Explicit event fixtures cover scrolling, movement, multiple fingers and cancellation without selection. Failed resolution offers Retry. CI runs `tests/ui/discovery.spec.js` in both browser engines; `tests/mobile-live/autocomplete.spec.js` uses iPhone-sized WebKit with real deployed English/Spanish/face-name suggestions and delayed genuine API responses, verifying unchanged test-profile ownership. These are engine/emulation checks, not physical iPhone or virtual-keyboard verification.

**UC-27:** Discover autocomplete click/tap and arrow+Enter open the shared card detail directly. Actual owned printings expose their own tag editor; unowned cards require explicit Add. Add/remove classification and location assignments survive reload, shortfalls remain nonblocking, and tag links filter by stable IDs. Ownership refresh failure/retry, save conflict/retry and late selection after leaving Discover are covered by `tests/card-ownership.test.js` and `tests/ui/discovery.spec.js`. **LIVE-08** (`tests/live/discovery-tags.spec.js`) adds/removes a temporary tag through this path in reserved keeper-tags, checks reload and clickable filtering, then restores its original assignments and verifies ownership/provenance are unchanged. LIVE-07 verifies direct keyboard opening for unowned cards and explicit Spanish printing review. No owner writes are used.

| ID      | Observable outcome                                                                                                                                                                                                                     | Evidence                                                         |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| UC-24   | Exact Piracy first; one English result per Oracle identity; accentless Spanish and face-name aliases; ambiguous matches remain distinct; deterministic bounded fuzzy fallback; exact printing/language picker independent of discovery | `tests/discovery.test.js`, `tests/ui/discovery.spec.js`, LIVE-07 |
| UC-25   | Debounce, bounded cache and shared in-flight work; old input cannot overwrite new suggestions; version/checksum/weekly background refresh, stale fallback and cold failure; full printing database never sent to browser               | `tests/discovery.test.js`, `tests/ui/discovery.spec.js`, LIVE-07 |
| UC-26   | Accessible combobox keyboard/IME/touch lifecycle, loading/no-result/error/retry, mobile width, Escape/Tab and mode changes; English rules while reviewing a Spanish printing                                                           | `tests/ui/discovery.spec.js`, LIVE-07                            |
| LIVE-07 | Actual deployed bulk index and Scryfall canonical cards: Piracy ranking, relampa autocomplete, Fire/Ice face alias, repeated queries/payload timing, English dedupe and Spanish printing review without ownership writes               | `tests/live/discovery.spec.js`                                   |

The bulk builder also validates real Piracy and Relámpago before publishing a snapshot. Language coverage depends on Scryfall’s available printed names, not machine translation; unavailable translations cannot be guessed.

| Use case | Expected behavior                                                                                                                                                                                                                                                                                                                                  | Verification                                                                                     |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| UC-33    | Complete compact multilingual worker search preserves ranking, face aliases, accent normalization, fuzzy/substring coverage and alias explanation. Verified IndexedDB reload, weekly checks, checksum/schema rejection, atomic update, stale-good retention, server fallback, native input/IME/touch cancellation and private ownership isolation. | tests/hybrid-search.test.js; tests/ui/hybrid-search.spec.js; tests/ui/discovery.spec.js; LIVE-11 |
| UC-34    | Selected UUID printing/Oracle verifies English paper data without names loading. Fresh individual printing reuse across result batches and bounded browser detail cache preserve 24h TTL, retry failures, exact-printing review and explicit ownership confirmation.                                                                               | tests/hybrid-search.test.js; tests/ui/hybrid-search.spec.js; LIVE-11                             |

LIVE-11 runs on the published service with keeper-tags, downloads the genuine public index, checks English/Spanish/Japanese/face suggestions locally, opens a card by native tap, reopens from browser cache and reloads the verified index without redownloading. It verifies unchanged owned rows and clears test-browser history. Performance events distinguish cold download/decode/build, cached restore, local search and render; no physical-device verification is implied.

| Use case | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Coverage                                                                                                                                             |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| UC-35    | Selecting autocomplete or recent-search entries opens one dedicated page immediately; shared grid/Home tiles use UC-CARD-ART first, with known heading/loading status and no singleton results/modal. Query submit retains search results. Back restores view, filters, results and focus; Forward/reload/copied links verify exact identity. Invalid/absent owned entries remain unavailable; slow/error/rapid navigation cannot reopen a stale selection. English discovery, foreign printing choice, explicit Add, ownership/tag editing and soft shortfalls remain intact. | tests/card-page.test.js; tests/ui/card-page.spec.js; tests/ui/discovery.spec.js; tests/ui/interactions.spec.js; tests/ui/tags.spec.js; LIVE-07/08/12 |

| Use case | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Coverage                                                                                                                                                                                                                                                       |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UC-36    | Optional server recognition supports interchangeable visual and recognition adapters without changing scanner controls. Model scores retain adapter-specific meaning and versioned calibration. Only canonical optional candidates can enter review after explicit choice; unknown, failure, busy, unapproved confirmation and canceled requests add no copy. Source download and transient-processing notice remain available. Final ownership review is required. | recognition/tests/test_contract.py; recognition/tests/test_transport.py; tests/backend-recognition.test.js; tests/ui/backend-scanner.spec.js (Chromium/WebKit). Actual model evidence: tests/performance/EVIDENCE.md. Physical calibration remains unverified. |

| UC-37 | Automatic browser/Lambda selection with no picker; entry preparation, cache reuse and integrity repair, bounded stalled-download feedback, service failure, recovery and cancellation; explicit printing choice always required. No periodic warming. | tests/hybrid-recognition.test.js; tests/visual-assets.test.js; tests/browser-recognition.test.js; tests/ui/backend-scanner.spec.js; tests/ui/app.spec.js; tests/performance/EVIDENCE.md; LIVE-14. |

## Phone scanning and durable batch review (September 9)

- **UC-SCAN-SUGGEST:** supported identity automatically queues a canonical suggested printing; exact footer/language evidence ranks before regular editions and promos; unknown or unapproved confirmation never adds a copy or success. Printing/finish/condition remain editable on final review, with explicit ownership. Tests: `printing-suggestion.test.js`, `phone-review.spec.js`, backend/scanner suites and LIVE-04/14.
- **UC-SCAN-IMPORT / UC-IMPORT-TEXT:** Scan and pasted lists use the existing account-backed Import page, preserving a concurrent URL draft and separate same-day batches. Back restores the latest origin; ID links/reload retain the selected draft. Fifty lines save atomically, duplicate exact printings sum, and lost-stage/Add responses and double taps do not duplicate or resurrect imports. Quantities, printing, finish, condition, tags, alternatives, delete and Clear persist. Corrupt local journals block replacement capture while saved account drafts remain usable. Server UTC timestamps preserve creation and no-op/retry dates. Tests: `import-drafts.test.js`, `import-dates.test.js`, `phone-review.spec.js`, `recognition-race.test.js`, LIVE-15. `review-batches.test.js` retains only legacy submitted-journal migration coverage.
- **UC-CAMERA-QUALITY:** unsupported capabilities and OverconstrainedError retain safe fallback; late permission cannot replace a newer session; native guide geometry and bounded best-frame selection preserve independent physical departure/stability. No settings wall, forced torch or unresolved success. Tests: `camera-quality.test.js`, scanner UI cancellation/identical-copy/backlog cases. Recorded replay and physical-device limitations: `tests/performance/PHONE-EVIDENCE.md`.

Persisted receipt and no-op comparisons use structural JSON values: database map-field order is immaterial, while row/alternative order and actual values remain significant. This fixes the real DynamoDB LIVE-15 failure where an unchanged stage retry was mistaken for changed input. Reordered-storage unit coverage also preserves no-op draft/assignment timestamps. LIVE-08 checks that a real tag edit advances only the edited entry's modification time while ownership/provenance stay unchanged; repeating the restored assignment is a timestamp-preserving no-op. LIVE-15 verifies the actual cloud stage replay and no-op draft save before atomic Add.

## Catalogue artwork and card actions

- **UC-CARD-PROTOTYPE:** Hover aims for 2x with 16px edges; inspection fits the full portrait up to 3x with permanent 24px side/32px vertical safe-area-adjusted dismissal gutters. Truthful percentage/Reset, source-origin spring, resize and canceled pinch stay bounded. Left collection information and separate right glass actions remain reachable. Full-card upgrade failure/departure retains the previous image. `tests/prototype/geometry.spec.js` and `fit-fonts.spec.js` cover Chromium/WebKit, four surfaces, 320/390/landscape/desktop, safe-area simulation and dense grids. Browser emulation is not physical-device verification.

- **UC-CARD-ART:** Image-only collection/deck/tag/catalogue/Home tiles retain escaped accessible identity. Hover/inspector show collection-specific quantity, finish/condition, plain tags, useful provenance and soft shortfalls; printed card metadata stays in the image and dedicated details page. Route-preserving fitted inspection supports zoom/pan, Reset, consumed outside dismissal, Escape/focus and detail-page Back. Quantity failure/retry/persistence and tag navigation remain covered by `tests/ui/card-actions.spec.js`, discovery/interactions suites and LIVE-01/09/12/17/18. Keyboard Escape followed immediately by Enter remains a fresh action.
- **UC-CARD-ACTIONS:** Full-size translucent drag copy, unchanged original through background refresh, pickup-centered annular hit sectors, a clear stationary source, soft glass label backing, relevant tags first and frozen geometry; labels too long for the band remain in More tags, one-copy source-location moves, classifications, keyboard editing, cancel without write/navigation, account-isolated recency and searchable overflow. Atomic rollback, replay after later edits, cross-account rejection, honest pending catalogue provenance and explicit Add. `tests/card-actions.test.js`, `tests/tags.test.js`, `tests/import-drafts.test.js`, `tests/ui/card-actions.spec.js`, LIVE-17/18.
- **UC-CARD-RETRY:** A lost post-commit response and reload reuse the same operation ID; definitive conflicts allow a fresh choice; corrupt/unavailable storage blocks replacement; account and view changes reject late effects. The UI suite exercises actual SQLite services for commit/retry and isolated browser save ports for account/storage boundaries. LIVE-17/18 interrupt delivery of one real cloud commit response, then verify actual DynamoDB persistence. LIVE-17 verifies a second reserved account cannot address the first account's entry.
- **UC-CARD-TOUCH:** Native browser taps, controlled pointer pinch/pan, movement before long press, edge fit and pending-only tag changes. Chromium/WebKit fixtures distinguish engine behavior from physical touch hardware. LIVE-18 follows a direct sector or More tags when the phone wheel overflows, including Review & add; the local phone overflow case verifies saved review without ownership. It uses native WebKit taps on owned/catalogue inspectors and Import controls, with controlled touch hold/move/release events for direct wheel pickup. It does not claim physical long-press or pinch verification.
- **UC-CARD-PERF:** A thousand catalogue fixture cards produce no idle animation frames or per-card `will-change`, while hover stops cleanly. See `tests/performance/CARD-ACTIONS.md`. This is a desktop fixture measurement, not a phone frame-rate or network benchmark.
- **UC-CARD-POINTER:** A burst of pointer moves schedules at most one pending application frame and performs no synchronous hover geometry reads. Leaving and canceling pickup stop pending work. Direct pickup blocks new text selection only while held and restores normal selection on release. `tests/ui/card-actions.spec.js`. Actual-app traversal, dwell-boundary and paced-drag profiles use 1,000 isolated fixture rows in Chromium/WebKit; physical cursor latency remains unverified.

The WebKit CI selection explicitly includes the card-action suite. Live cleanup clears assignments, only newly staged capture drafts and test-owned rows in keeper-e2e, then removes its temporary tags. Existing keeper-tags/import source fixtures and owner data remain untouched.

- **UC-CARD-TILES / UC-CARD-WHEEL:** Native shared-tile clicks/taps keep their route; 200% preview corner/center matrix sweeps use a stable untransformed hit plane. A continuous 300ms hover starts the 200% lift; information is immediate. Departure at 299ms cancels the lift and re-entry starts a fresh 300ms dwell; reduced motion keeps zoom without animated tilt. All four viewport edges at 1280, 390, 320 and landscape widths retain full wrapped labels, viewport bounds and translucent proportional ghost screenshots. Pending previews remain unowned. `tests/ui/card-actions.spec.js`, LIVE-17/18.

Click/tap retains the source slot and visible grid, enlarges around the source center and shifts only for dismissal gutters. Ultrawide left/middle/right and scrolled application cases verify this placement, nearby info/actions, outside-click consumption and restored scroll/focus.

Hover and expanded zoom use a brief damped spring: a small overshoot then exact 200%/300% settling. Nested reveal and tilt layers keep cursor tilt continuous; reduced-motion preferences skip the reveal animation.

Active pickup suppresses text selection and native image callouts only for the gesture; pointer release, cancellation, lost capture and navigation restore normal interaction. A gesture-created selection is cleared only when anchored inside its card and no prior selection existed.

- **UC-FONTS:** Self-hosted licensed display/body faces load from the release origin. Loaded, failed and delayed fonts preserve inspector fit and fixed active wheel targets; actual Chromium platform-font checks verify Cinzel is used. `tests/prototype/fit-fonts.spec.js`.

## Frontend deployment (in progress)

- **UC-DEPLOY-FAST / DEPLOY-01–04:** Compare the proposed tree with the actually published frontend. Only explicitly supported presentation changes may reuse an independently identified application backend and verified immutable model/runtime assets. Unknown inputs or incomplete metadata require the full path. Documentation/test-only changes run checks without republishing application code. API compatibility, source completeness, publication ordering, failure recovery and reserved-profile live checks remain required before delivery. Classifier coverage: `tests/release-plan.test.js`; pipeline integration and live speed measurements are pending.
